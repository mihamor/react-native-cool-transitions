import styled from "styled-components";
import { Colours } from "~constants";
import { HeadingLarge } from "~styles/containers";

export const AppName = styled(HeadingLarge)`
  color: ${Colours.secondary};
`;